using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Login : MonoBehaviour
{
    public GameObject email;
    public GameObject password;

    public void moveDown()
    {
         //panel.transform.Translate = Vector3.Lerp(0,-760,Time.deltaTime * 1);
         //panel.transform.Translate = Vector3.Lerp (transform.position, Vector3 (next.transform.position.x, next.transform.position.y, z), Time.deltaTime * 2);
    }

    public void login()
    {
        
    }
}
